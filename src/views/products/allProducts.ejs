<!DOCTYPE html>
<html>
    <%- include('../partial/head') %>
    <body>
        <%- include('../partial/header') %>
        <main>
            <h1> <%= title %></h1>
            <h2 class="titulo">Todos nuestros productos</h2>
            <section class="article-product-section">

                <% products.forEach((product) => { %>
                    <%- include('../partial/articleProduct', {
                        ...product.dataValues,
                    }) %>
                <% }) %>

            </section>

        </main>
        <%- include('../partial/footer') %>
    </body>
</html>