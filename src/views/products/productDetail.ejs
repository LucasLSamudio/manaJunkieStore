<!DOCTYPE html>
<html>
    <%- include('../partial/head') %>
    <body>
        <%- include('../partial/header') %>
        <main>
            <%- include('../partial/boxProductDetail',{
                ...product.dataValues,
            }) %>
            <h3 class="titulo">Productos relacionados</h3>
            <section class="article-product-section">
                <% relationalProducts.forEach(product => { %>
                    <%- include('../partial/articleProduct',{
                        ...product.dataValues,
                    }) %>
                <% }) %>
            </section>
        </main>
        <%- include('../partial/footer') %>
    </body>
</html>